import os
os.chdir("C:\\")
for textonly in os.listdir():
    if str(textonly).split('.')[-1] == 'txt':
        # .txt을 가진 파일만 출력
        unit_path = str(os.getcwd() + '\\' + str(textonly) )
        print(unit_path)
